const RAYON_MAX = 10;

var canvas = document.getElementById("canvas");
var c = canvas.getContext("2d");
var tx = window.innerWidth;
var ty = window.innerHeight;

class Cercle {
    constructor() {
        this.hauteur = floor(Math.random() * RAYON_MAX);
        this.largeur = largeur;
        this.x = floor(Math.random() * tx);
        this.y = floor(Math.random() * ty);
    }

}

class Cercles {


    constructor(nb_cercles)
    {

    }

}